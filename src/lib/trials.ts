// src/lib/trials.ts
import { products } from "@/config/products";

// Tomorrow: implement a function that
// takes userId and returns product trial status list
export async function getUserProductStatuses(userId: string) {
  // TODO: query ProductGrant by userId
  // TODO: map over products and compute status per product
  return [];
}